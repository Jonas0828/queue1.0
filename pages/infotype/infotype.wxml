<block wx:for="{{list}}" wx:key="id">
  <view class="item" bindtap="updateInfo">
    <view class="title">{{item.name}}</view>
    <view class="flag">{{item.complete.msg}}</view>
    <image class="arrow" src="../resource/picture/user/9.png"></image>
  </view>
</block>